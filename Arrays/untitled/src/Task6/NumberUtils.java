package Task6;// **************
// *** Task 6 ***
// **************

public class NumberUtils {
    // No error found
    // Method already written in one line
    public boolean isEven(int number) {
        return (number % 2 == 0);
    }
}
